---
phase: PW-02-site-islands
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/islands/GameIsland.tsx
  - src/components/home/Work.astro
  - src/data/projects.js
  - src/lib/seo.ts
  - src/pages/index.astro
  - tests/game-island.test.ts
autonomous: true
requirements:
  - MIGR-05
  - MIGR-07
  - OPT-06

must_haves:
  truths:
    - "Phaser bundle is not loaded until user clicks game start control"
    - "After click, Phaser game runs with same scene behavior as PhaserGame.jsx"
    - "Each project in Work section has visible description text for crawlers"
    - "Project descriptions are present in projects data and rendered in HTML"
  artifacts:
    - path: src/components/islands/GameIsland.tsx
      provides: "Click-gated Phaser MIGR-07"
    - path: src/data/projects.js
      provides: "Project descriptions OPT-06"
  key_links:
    - from: GameIsland.tsx
      to: src/game/scenes/GameScene.js
      via: "dynamic import after click"
---

<objective>
Add click-gated Phaser GameIsland and complete portfolio (Work) section SEO with per-project descriptions. Migrates game + portfolio concerns from legacy MainLayout.

Purpose: MIGR-07 lazy Phaser; OPT-06 project meta in static HTML.
Output: GameIsland.tsx, projects.js descriptions, optional ItemList JSON-LD, game chunk gate test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-02-site-islands/02-CONTEXT.md
@.planning/phases/PW-02-site-islands/02-RESEARCH.md
@src/game/PhaserGame.jsx
@src/game/GameTooltip.jsx
@src/game/scenes/GameScene.js
@src/components/home/Work.jsx
@src/components/home/ProjectCard.jsx
@src/data/projects.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GameIsland click-gated Phaser load (MIGR-07)</name>
  <files>src/components/islands/GameIsland.tsx, src/pages/index.astro, tests/game-island.test.ts</files>
  <read_first>PhaserGame.jsx, GameTooltip.jsx, 02-CONTEXT.md D-20</read_first>
  <behavior>
    - Test: GameIsland module does not statically import 'phaser' at top level (grep/isolate test)
    - Test after build: dist has phaser chunk file but initial index.html script tags do not synchronously load phaser before interaction (document structure / manual UAT)
  </behavior>
  <action>
    Create GameIsland.tsx: initial UI shows GameTooltip/placeholder; on click set state and dynamic import phaser + GameScene; create/destroy game on unmount (mirror PhaserGame cleanup).

    Add tests/game-island.test.ts: read GameIsland.tsx source — assert no top-level `from 'phaser'` / `from "phaser"`; assert `import(` present for dynamic load.

    Mount `<GameIsland client:only="react" />` only in index.astro (not BlogLayout).

    Do NOT include PhaserGame in HomeLayout globally — blog routes must not load game.
  </action>
  <acceptance_criteria>
    GameIsland.tsx has no top-level phaser import; dynamic import() present; phaser chunk not loaded until user click (manual UAT per VALIDATION.md).
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/game-island.test.ts && yarn build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Portfolio descriptions and semantic HTML (OPT-06)</name>
  <files>src/data/projects.js, src/components/home/Work.astro, src/lib/seo.ts</files>
  <read_first>projects.js, ProjectCard.jsx, 02-CONTEXT.md D-23</read_first>
  <action>
    Add `description` field (Chinese, 1–2 sentences, tech keywords) to each entry in projects.js.

    Update Work.astro / ProjectCard.astro to render description in visible paragraph (not display:none).

    Optional: add `projectsJsonLd(projects)` ItemList or per-item CreativeWork to home seo JSON-LD array.

    Ensure external project links use rel="noopener noreferrer".
  </action>
  <acceptance_criteria>
    Each project in projects.js has description field; dist/index.html contains visible description text for portfolio items (e.g. 心晴).
  </acceptance_criteria>
  <verify>
    <automated>yarn build && grep -q '心晴' dist/index.html && grep -q 'description' dist/index.html</automated>
  </verify>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW02-03 | Tampering | Phaser canvas | accept | Local game only; no user data |
| T-PW02-04 | Spoofing | project external links | mitigate | rel noopener on outbound links |
</threat_model>

<verification>
yarn build; manual: network tab shows no phaser until click; work section has descriptions.
</verification>

<success_criteria>
MIGR-07 click-gated Phaser; OPT-06 project descriptions in HTML.
</success_criteria>

<output>
Create `.planning/phases/PW-02-site-islands/02-02-SUMMARY.md` when done
</output>
