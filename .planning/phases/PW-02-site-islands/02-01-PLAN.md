---
phase: PW-02-site-islands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/layouts/HomeLayout.astro
  - src/components/home/Main.astro
  - src/components/home/About.astro
  - src/components/home/Skill.astro
  - src/components/home/Work.astro
  - src/components/home/Contact.astro
  - src/components/home/FeaturedPosts.astro
  - src/components/islands/HomeMotion.tsx
  - src/components/islands/ParticleIsland.tsx
  - src/components/islands/MouseTrailIsland.tsx
  - src/lib/seo.ts
  - src/lib/featured.ts
  - src/loaders/posts-loader.ts
  - tests/home-seo.test.ts
autonomous: true
requirements:
  - MIGR-05
  - MIGR-06
  - SEO-03
  - OPT-05

must_haves:
  truths:
    - "D-16: index.astro outputs static Main/About/Skill/Work/Contact HTML (crawler-visible)"
    - "D-18: Home uses HomeLayout.astro; blog routes keep BlogLayout.astro"
    - "D-19: HomeMotion.tsx client:visible runs GSAP ScrollTrigger reveal + Lenis smooth scroll"
    - "Home page renders all sections (hero, about, skills, work, contact) as static HTML matching existing home components"
    - "GSAP ScrollTrigger reveal-up animations run via HomeMotion client:visible island"
    - "Lenis smooth scroll initialized from HomeMotion without breaking anchor navigation"
    - "Home page JSON-LD includes WebSite and Person types"
    - "Three featured/recent blog posts appear as static links with descriptive anchor text"
    - "ParticleCanvas and MouseTrail ambient effects render on home (D-21 visual parity)"
  artifacts:
    - path: src/layouts/HomeLayout.astro
      provides: "Home chrome with Header/Footer MIGR-05"
    - path: src/components/islands/HomeMotion.tsx
      provides: "GSAP + Lenis island MIGR-06"
    - path: src/lib/seo.ts
      provides: "personJsonLd SEO-03"
      contains: "Person"
    - path: src/components/islands/ParticleIsland.tsx
      provides: "ParticleCanvas ambient effect D-21"
    - path: src/components/islands/MouseTrailIsland.tsx
      provides: "MouseTrail ambient effect D-21"
  key_links:
    - from: src/pages/index.astro
      to: src/layouts/HomeLayout.astro
      via: "layout wrapper"
    - from: src/pages/index.astro
      to: HomeMotion.tsx
      via: "client:visible"
---

<objective>
Replace Phase 1 placeholder homepage with full static home sections plus HomeMotion island (GSAP ScrollTrigger + Lenis). Add WebSite/Person JSON-LD and static featured blog block per SEO-03 and OPT-05.

Purpose: Restore crawlable full homepage while deferring Phaser to plan 02-02.
Output: HomeLayout, home section Astro components, HomeMotion.tsx, seo extensions, featured helper, home SEO test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-02-site-islands/02-CONTEXT.md
@.planning/phases/PW-02-site-islands/02-RESEARCH.md
@.planning/phases/PW-02-site-islands/02-PATTERNS.md
@.planning/phases/PW-01-astro-ssg/01-SUMMARY.md
@src/pages/_Home.jsx
@src/components/home/Main.jsx
@src/components/home/About.jsx
@src/components/home/Skill.jsx
@src/components/home/Work.jsx
@src/components/home/Contact.jsx
@src/hooks/useScrollToTop.js
@src/layouts/BaseLayout.astro
@src/pages/index.astro
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Home SEO helpers and tests (SEO-03)</name>
  <files>src/lib/seo.ts, tests/home-seo.test.ts</files>
  <read_first>src/lib/seo.ts, BaseLayout.astro, 02-CONTEXT.md D-24</read_first>
  <behavior>
    - Test: personJsonLd returns @type Person with name from SITE.author
    - Test: home seo input produces both WebSite and Person in JSON-LD array
    - Test after build: dist index.html contains application/ld+json with Person and WebSite
  </behavior>
  <action>
    Add `personJsonLd()` to seo.ts using SITE.name, SITE.author, SITE.url, SITE.description. Extend home page seo usage to pass both website and person schemas via BaseLayout (filter array like Phase 1).

    RED: tests/home-seo.test.ts unit tests for personJsonLd + integration grep after build.

    GREEN: Wire into index.astro HomeLayout seo prop.
  </action>
  <acceptance_criteria>
    personJsonLd returns Person schema; home page JSON-LD includes WebSite + Person; dist/index.html contains both types after build.
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/home-seo.test.ts && yarn build && grep -q 'Person' dist/index.html && grep -q 'WebSite' dist/index.html</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: HomeLayout and static section components (MIGR-05)</name>
  <files>src/layouts/HomeLayout.astro, src/components/home/*.astro</files>
  <read_first>MainLayout.jsx, Header.jsx, Footer.jsx, home/*.jsx, src/index.css section classes</read_first>
  <action>
    Create HomeLayout.astro wrapping BaseLayout: import global styles, render Header + Footer (adapt from React components to Astro or reuse React Header/Footer with client:load if faster — prefer Astro/static for SEO).

    Port Main, About, Skill, Work, Contact to `.astro` components preserving Tailwind classes, ids (`home`, `about`, `work`), and `reveal-up` classes for GSAP targets. Import data from `src/data/about.js`, `skills.js`, `projects.js` (projects descriptions added in 02-02 if missing — use title-only fallback here).

    Replace index.astro placeholder with HomeLayout + all sections in order matching `_Home.jsx`.
  </action>
  <acceptance_criteria>
    dist/index.html contains id="about" and id="work"; all five home sections present in static HTML with reveal-up classes on targets.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && grep -q 'id="about"' dist/index.html && grep -q 'id="work"' dist/index.html</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Featured posts static block (OPT-05)</name>
  <files>src/lib/featured.ts, src/loaders/posts-loader.ts, src/components/home/FeaturedPosts.astro</files>
  <read_first>getFeaturedPosts.js, 02-CONTEXT.md D-17</read_first>
  <action>
    Add `featured: z.boolean().default(false)` to posts-loader schema and map from parsePost.

    Create `getHomeFeaturedPosts()` in featured.ts: featured first, fill to 3 with recent published posts.

    Create FeaturedPosts.astro section with descriptive link text (title + short description), href `import.meta.env.BASE_URL + 'blog/' + slug + '/'`.
  </action>
  <acceptance_criteria>
    Home page HTML contains ≥3 blog post links with descriptive anchor text; featured posts prioritized when flagged.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && grep -c '/blog/' dist/index.html | awk '$1>=3'</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: HomeMotion island — GSAP + Lenis + ambient effects (MIGR-06, D-21)</name>
  <files>src/components/islands/HomeMotion.tsx, src/components/islands/ParticleIsland.tsx, src/components/islands/MouseTrailIsland.tsx, src/pages/index.astro</files>
  <read_first>_Home.jsx ANIMATION_CONFIG, useScrollToTop.js, ParticleCanvas.jsx, MouseTrail.jsx, 02-RESEARCH.md client:visible, 02-CONTEXT.md D-21</read_first>
  <action>
    Create HomeMotion.tsx migrating GSAP ScrollTrigger reveal logic from _Home.jsx (respect prefers-reduced-motion). Initialize Lenis from useScrollToTop pattern; ensure `#about` / `#work` anchor clicks still work.

    Migrate ParticleCanvas and MouseTrail to separate `client:visible` islands (ParticleIsland.tsx, MouseTrailIsland.tsx) — port logic from `src/components/common/ParticleCanvas.jsx` and `MouseTrail.jsx`. D-21 requires current-site visual parity; do not skip ambient effects.

    Mount in index.astro: `<HomeMotion client:visible />` plus ParticleIsland and MouseTrailIsland after static sections. Static HTML must remain in page source.

    Verify in dev: scroll reveals fire; particle + trail effects visible; no console errors.
  </action>
  <acceptance_criteria>
    HomeMotion chunk exists in dist/_astro; GSAP targets use .reveal-up; Lenis does not break #about/#work anchors; ParticleCanvas and MouseTrail effects render on home.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && ls dist/_astro/*HomeMotion* 1>/dev/null && ls dist/_astro/*Particle* 1>/dev/null</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React island → DOM | Client-side animation mutates visibility only |
| GSAP → third-party | CDN not used; npm gsap package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW02-01 | Denial of service | GSAP/Lenis island | mitigate | client:visible defers JS; respect prefers-reduced-motion |
| T-PW02-02 | Tampering | Lenis anchor scroll | mitigate | Test #about/#work navigation after Lenis init |
</threat_model>

<verification>
yarn test tests/home-seo.test.ts; yarn build; home HTML contains section ids and 3 blog links.
</verification>

<success_criteria>
MIGR-05 home sections live; MIGR-06 GSAP/Lenis via island; SEO-03 Person JSON-LD; OPT-05 featured block.
</success_criteria>

<output>
Create `.planning/phases/PW-02-site-islands/02-01-SUMMARY.md` when done
</output>
