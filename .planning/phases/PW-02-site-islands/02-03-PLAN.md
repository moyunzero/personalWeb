---
phase: PW-02-site-islands
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - README.md
  - package.json
  - index.html
  - src/main.jsx
  - src/App.jsx
  - src/components/blog/BlogEditor.jsx
  - src/routes/index.js
  - tests/editor-route.test.ts
autonomous: true
requirements:
  - MIGR-08
  - OPT-05

must_haves:
  truths:
    - "D-22: ChatTrigger/ChatPanel not migrated; no chat island on home or blog"
    - "D-27: @astrojs/sitemap covers home URL; no posts-index.json reintroduced"
    - "/blog/editor returns 404 in production build — no editor HTML"
    - "README documents yarn blog:new and yarn notion:sync only — no web editor"
    - "Vite SPA entry (index.html main.jsx) not required for yarn build"
    - "Legacy BlogEditor component and DEV editor route removed from codebase"
  artifacts:
    - path: README.md
      provides: "Publishing docs MIGR-08"
    - path: tests/editor-route.test.ts
      provides: "No editor route verification"
  key_links:
    - from: package.json build
      to: astro build
      via: "no vite in build script"
---

<objective>
Remove web blog editor and Vite SPA production remnants. Update README for CLI + Notion publishing only.

Purpose: MIGR-08; complete migration off dual-stack dev confusion.
Output: deleted BlogEditor, cleaned routes, README, editor 404 test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-02-site-islands/02-CONTEXT.md
@src/components/blog/BlogEditor.jsx
@src/routes/index.js
@README.md
@index.html
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove BlogEditor and verify /blog/editor 404 (MIGR-08)</name>
  <files>src/components/blog/BlogEditor.jsx, src/routes/index.js, tests/editor-route.test.ts</files>
  <read_first>BlogEditor.jsx, routes/index.js DEV editor block</read_first>
  <behavior>
    - Test after build: no dist/personalWeb/blog/editor/index.html (or path absent)
    - Test: grep dist does not contain BlogEditor chunk name
  </behavior>
  <action>
    Delete BlogEditor.jsx and remove lazy import from routes/index.js (if routes file kept).

    Ensure Astro does not define blog/editor route — missing path → 404.astro / GH Pages 404.

    D-22: Do not mount ChatTrigger/ChatPanel; confirm no VITE_CHAT_API_URL in CI/deploy workflow.

    RED/GREEN: tests/editor-route.test.ts asserting no editor HTML in dist.
  </action>
  <acceptance_criteria>
    No dist path for blog/editor; editor-route test passes; BlogEditor.jsx deleted from codebase.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && yarn test tests/editor-route.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: README and SPA entry cleanup</name>
  <files>README.md, index.html, src/main.jsx, package.json</files>
  <read_first>README.md publishing section, 02-CONTEXT.md D-25 D-26</read_first>
  <action>
    Update README: publishing via `yarn blog:new` and `yarn notion:sync`; remove web editor instructions.

    Remove or archive Vite SPA bootstrap: delete `index.html` redirect script at repo root OR add comment that Astro is production entry; remove `src/main.jsx` from active path if safe (keep vite.config for optional dev legacy only if needed — prefer document `yarn dev` = astro dev).

    Remove unused `_Home.jsx` / `_Blog.jsx` / `_BlogDetail.jsx` if fully migrated; otherwise delete in this task after parity confirmed.

    Do not remove gsap/phaser/react deps — still used by islands.

    D-27: Do not add posts-index.json; home remains in sitemap via existing @astrojs/sitemap integration from Phase 1.
  </action>
  <acceptance_criteria>
    README documents yarn blog:new and yarn notion:sync only; no blog/editor references; package.json build is astro-only.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && ! grep -qi 'blog/editor' README.md && grep -qi 'notion:sync' README.md && grep -qi 'blog:new' README.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW02-05 | Information disclosure | removed editor | mitigate | Delete editor code; 404 on route |
</threat_model>

<verification>
yarn test tests/editor-route.test.ts; README accurate; yarn build unchanged.
</verification>

<success_criteria>
MIGR-08 complete; no editor route; README CLI-only publishing.
</success_criteria>

<output>
Create `.planning/phases/PW-02-site-islands/02-03-SUMMARY.md` when done
</output>
