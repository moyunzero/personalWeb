---
phase: PW-01-astro-ssg
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - astro.config.mjs
  - package.json
  - src/pages/index.astro
  - src/pages/404.astro
  - src/pages/robots.txt.ts
  - src/pages/sitemap.xml.ts
  - public/404.html
  - scripts/generate-static.mjs
  - .github/workflows/deploy.yml
  - tests/sitemap.test.ts
autonomous: true
requirements:
  - MIGR-04
  - SEO-04

must_haves:
  truths:
    - "dist contains sitemap-index.xml (or sitemap.xml) listing home, blog index, and all published posts with trailing slashes"
    - "dist contains robots.txt pointing to sitemap URL"
    - "Placeholder homepage has H1 and ~200 words crawlable text linking to blog"
    - "GitHub Actions build runs yarn build with astro only — no vite build or generate-static in chain"
    - "404 handling does not redirect to removed SPA index.html"
  artifacts:
    - path: src/pages/robots.txt.ts
      provides: "Prerendered robots.txt SEO-04"
      contains: "Sitemap:"
    - path: src/pages/index.astro
      provides: "Placeholder home D-02"
      contains: "<h1"
    - path: .github/workflows/deploy.yml
      provides: "CI astro build MIGR-04"
  key_links:
    - from: astro.config.mjs
      to: "@astrojs/sitemap"
      via: "sitemap integration filter drafts"
      pattern: "sitemap"
    - from: src/pages/robots.txt.ts
      to: sitemap-index.xml
      via: "new URL('sitemap-index.xml', site)"
      pattern: "sitemap-index"
    - from: .github/workflows/deploy.yml
      to: package.json build script
      via: "yarn build"
      pattern: "yarn build"
---

<objective>
Complete Phase 1 delivery: sitemap and robots generation, placeholder static homepage, 404 handling for GitHub Pages, retire Vite/generate-static from production build chain, and switch CI to Astro-only deploy per MIGR-04, SEO-04, D-01/D-02/D-08/D-13/D-14/D-15.

Purpose: End-to-end static deploy with discoverable URLs and no dual-build dist merge.
Output: robots endpoint, sitemap config, final index.astro, 404 pages, deploy.yml, sitemap tests, package.json cleanup.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-01-astro-ssg/01-CONTEXT.md
@.planning/phases/PW-01-astro-ssg/01-RESEARCH.md
@.planning/phases/PW-01-astro-ssg/01-PATTERNS.md
@public/404.html
@index.html
@scripts/generate-static.mjs
@.github/workflows/deploy.yml
@package.json
@src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sitemap integration and robots.txt endpoint (SEO-04, D-13)</name>
  <files>astro.config.mjs, src/pages/robots.txt.ts, tests/sitemap.test.ts</files>
  <read_first>01-RESEARCH.md Pattern 5, scripts/generate-static.mjs buildSitemap/buildRobots, 01-CONTEXT.md D-10 D-13, 01-RESEARCH.md Open Question 3 sitemap-index.xml</read_first>
  <behavior>
    - Test: after build, dist contains sitemap-index.xml or sitemap-0.xml
    - Test: sitemap includes /personalWeb/ home URL and /personalWeb/blog/ with trailing slashes
    - Test: sitemap URL count equals published non-draft post count + 2 (home + blog index)
    - Test: dist/personalWeb/robots.txt exists and references sitemap-index.xml
    - Test: no draft post slug appears in sitemap XML
  </behavior>
  <action>
    Configure @astrojs/sitemap in astro.config.mjs with filter excluding draft routes if any leak. Confirm trailingSlash always aligns sitemap entries with D-10.

    Create src/pages/robots.txt.ts per RESEARCH Pattern 5: export GET prerendered handler returning User-agent * Allow / and Sitemap line pointing to new URL('sitemap-index.xml', site).href — update from legacy /sitemap.xml per RESEARCH recommendation.

    RED: Create tests/sitemap.test.ts parsing built sitemap XML and robots.txt after yarn build. Compare post count via getCollection or grep content/posts draft false count.

    GREEN: Ensure integration emits sitemap on full build with blog routes from 01-02 present.

    Add prerendered src/pages/sitemap.xml.ts (or public redirect) that returns 301/alias to sitemap-index.xml so legacy `/sitemap.xml` and D-13 robots expectations remain satisfied alongside sitemap-index.xml.

    Remove sitemap.xml and robots.txt generation from scripts/generate-static.mjs execution path — either delete those functions' calls from main() or remove script from package.json build chain. Do not write public/posts-index.json per D-08.
  </action>
  <acceptance_criteria>
    yarn test tests/sitemap.test.ts passes; dist has robots.txt and sitemap files; draft slugs absent from sitemap.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && yarn test tests/sitemap.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Placeholder homepage and 404 handling (D-02, D-15)</name>
  <files>src/pages/index.astro, src/pages/404.astro, public/404.html</files>
  <read_first>01-CONTEXT.md D-02, 01-RESEARCH.md Placeholder Homepage, public/404.html, docs/SEO-MIGRATION-DESIGN.md §3</read_first>
  <action>
    Replace scaffold index with src/pages/index.astro per D-02: BaseLayout with SITE title/description, main bg-zinc-900, H1 with SITE.name, paragraph ~200 Chinese characters describing site purpose (日常、笔记与思考, static migration note), link a href to import.meta.env.BASE_URL + 'blog/' with trailing slash text 进入博客.

    Create src/pages/404.astro using BaseLayout (SEO-06): message 页面未找到, links to home and blog — no GSAP/Phaser; html lang=zh-CN via layout.

    Update public/404.html: remove rafgraph SPA redirect to React index.html (D-03/D-15). Replace with static minimal HTML matching 404.astro content or simple meta refresh to /personalWeb/ — must NOT load removed SPA entry. GitHub Pages uses dist/404.html for unknown URLs.

    Verify real routes /personalWeb/blog/ and /personalWeb/blog/{slug}/ serve directory index.html without relying on SPA fallback.
  </action>
  <acceptance_criteria>
    dist/personalWeb/index.html has h1 and ≥150 chars body text; dist/404.html has no window.location SPA redirect script to ?/ path.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && grep -q '<h1' dist/personalWeb/index.html && ! grep -q 'replaceState' dist/personalWeb/404.html && grep -q 'lang="zh-CN"' dist/personalWeb/404.html</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Production build chain and GitHub Actions switch (MIGR-04, D-01, D-03, D-14)</name>
  <files>package.json, .github/workflows/deploy.yml, scripts/generate-static.mjs</files>
  <read_first>.github/workflows/deploy.yml, package.json, 01-CONTEXT.md D-01 D-03 D-08 D-14, 01-PATTERNS.md package.json section</read_first>
  <action>
    Finalize package.json scripts: dev = astro dev, build = astro build only — no node scripts/generate-static.mjs && vite build per D-01/D-03. preview = astro preview. Keep blog:new and notion:sync unchanged.

    Update .github/workflows/deploy.yml Build step: run yarn build without VITE_CHAT_API_URL env block (placeholder home has no chat island Phase 1 per RESEARCH). Artifact path remains ./dist. No vite build invocation.

    Comment or strip scripts/generate-static.mjs posts-index.json and sitemap writes from any remaining script entry — file may remain for reference but must not run in CI. Confirm public/posts-index.json is not regenerated on build.

    Run full phase gate: yarn test && yarn build. Grep deploy.yml for yarn build and absence of vite build.
  </action>
  <acceptance_criteria>
    yarn test && yarn build exit 0; deploy.yml runs yarn build only; package.json build script is astro build without vite or generate-static.
  </acceptance_criteria>
  <verify>
    <automated>yarn test && yarn build && node -e "const p=require('./package.json'); if(p.scripts.build.includes('vite')||p.scripts.build.includes('generate-static')) process.exit(1)" && grep -q 'yarn build' .github/workflows/deploy.yml</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → dist artifact | CI builds public deploy bundle |
| robots.txt/sitemap | Public crawl directives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW01-08 | Information disclosure | deploy.yml env | mitigate | Remove unused VITE_CHAT_API_URL from Phase 1 build; no secrets in static bundle |
| T-PW01-09 | Tampering | CI workflow | mitigate | Single yarn build command; artifact path ./dist unchanged |
| T-PW01-10 | Denial of service | sitemap size | accept | ~96 posts static sitemap; acceptable for GH Pages |
</threat_model>

<verification>
Full suite yarn test && yarn build; sitemap count matches published posts; index page crawlable; CI workflow aligned.
</verification>

<success_criteria>
MIGR-04 CI uses astro build; SEO-04 sitemap and robots in dist; D-02 placeholder home live; D-08/D-03 production path has no Vite SPA or posts-index.json generation.
</success_criteria>

## Artifacts this phase produces

Final index.astro, 404.astro, public/404.html, robots.txt.ts, tests/sitemap.test.ts, updated deploy.yml and package.json — completes Phase 1 artifact set.

<output>
Create `.planning/phases/PW-01-astro-ssg/01-04-SUMMARY.md` when done
</output>
