---
phase: PW-01-astro-ssg
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/content.config.ts
  - src/loaders/posts-loader.ts
  - src/layouts/BaseLayout.astro
  - src/layouts/BlogLayout.astro
  - src/lib/blog.ts
  - src/lib/posts.ts
  - src/components/blog/BlogPostCard.astro
  - src/pages/blog/index.astro
  - src/pages/blog/[...slug].astro
  - tests/content.schema.test.ts
autonomous: true
requirements:
  - MIGR-02
  - MIGR-03

must_haves:
  truths:
    - "Blog UI is simplified SEO-first dark template, not pixel parity with Blog.jsx (D-04, D-05)"
    - "Every non-draft post has a static HTML page at /blog/{slug}/ with article body in source HTML"
    - "Blog list page renders published posts grouped by year at build time"
    - "Invalid frontmatter fails Zod schema validation in tests"
    - "Code blocks in markdown render as Shiki-highlighted pre/code in built HTML"
  artifacts:
    - path: src/content.config.ts
      provides: "posts collection with Zod schema MIGR-03"
      contains: "defineCollection"
    - path: src/loaders/posts-loader.ts
      provides: "Custom loader using parseFrontmatter D-09"
      contains: "parseFrontmatter"
    - path: src/pages/blog/[...slug].astro
      provides: "SSG blog detail per slug MIGR-02"
      contains: "getStaticPaths"
  key_links:
    - from: src/loaders/posts-loader.ts
      to: src/blog/frontmatter.js
      via: "import parseFrontmatter"
      pattern: "parseFrontmatter"
    - from: src/pages/blog/[...slug].astro
      to: src/content.config.ts
      via: "getCollection('posts')"
      pattern: "getCollection\\('posts'"
    - from: src/pages/blog/[...slug].astro
      to: src/layouts/BlogLayout.astro
      via: "layout wrapper + Content render"
      pattern: "BlogLayout"
---

<objective>
Implement Astro Content Collections for `content/posts/**/*.md` with Notion-compatible YAML parsing, blog list and detail SSG pages, and build-time Shiki code highlighting. Delivers crawlable blog HTML per MIGR-02/MIGR-03 and D-06/D-07/D-09.

Purpose: Replace posts-index.json + client fetch with build-time collection queries per D-08.
Output: content.config.ts, posts-loader, blog routes, minimal layout stubs, blog helpers, schema tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-01-astro-ssg/01-CONTEXT.md
@.planning/phases/PW-01-astro-ssg/01-RESEARCH.md
@.planning/phases/PW-01-astro-ssg/01-PATTERNS.md
@src/blog/parsePost.js
@src/blog/frontmatter.js
@src/blog/excerpt.js
@src/blog/getCategories.js
@src/blog/filterPosts.js
@src/blog/groupPostsByYear.js
@src/blog/utils.js
@scripts/lib/load-posts.mjs
@content/categories.json
@src/pages/Blog.jsx
@src/pages/BlogDetail.jsx
@src/components/blog/BlogPostCard.jsx
@src/components/blog/MarkdownContent.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Content Collection schema and posts loader (D-09, MIGR-03)</name>
  <files>tests/content.schema.test.ts, src/content.config.ts, src/loaders/posts-loader.ts</files>
  <read_first>01-RESEARCH.md Pattern 2, src/blog/parsePost.js, src/blog/frontmatter.js, scripts/lib/load-posts.mjs, 01-CONTEXT.md D-07 D-09</read_first>
  <behavior>
    - Test: valid fixture with title, slug, date, categories passes schema parseData
    - Test: missing title fails Zod validation
    - Test: loader assigns slug from frontmatter.slug or file stem
    - Test: draft: true entry stored but identifiable for filtering
  </behavior>
  <action>
    RED: Create tests/content.schema.test.ts importing collection schema or testing postsLoader logic with fixture strings. Include one Notion-style YAML sample (description:中文 without space) proving parseFrontmatter path works.

    GREEN: Create src/loaders/posts-loader.ts implementing Astro Loader API per RESEARCH Pattern 2. Read content/posts/*.md, call parseFrontmatter from src/blog/frontmatter.js (do not duplicate YAML). Map fields mirroring parsePost.js: title, slug, description (optional), date from date or publishDate, categories (array or single category), tags, draft, author default 墨韵, cover, body markdown in store.

    Create src/content.config.ts with defineCollection using postsLoader() and Zod schema: title z.string(), slug z.string(), description z.string().optional(), date z.string(), categories z.array(z.string()).default([]), tags z.array(z.string()).optional(), draft z.boolean().default(false), author z.string().default('墨韵'), cover z.string().optional(). Do NOT require description — OPT-02 is Phase 3.

    Filter drafts at query sites in later tasks, not by omitting from store (allows dev preview parity). Run full astro build early to catch YAMLException across 96 posts.
  </action>
  <acceptance_criteria>
    yarn test tests/content.schema.test.ts passes; astro sync or build loads all content/posts without YAML errors.
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/content.schema.test.ts && yarn astro sync</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Blog list page SSG with simplified dark UI (D-04)</name>
  <files>src/pages/blog/index.astro, src/lib/blog.ts, src/lib/posts.ts, src/components/blog/BlogPostCard.astro, src/layouts/BaseLayout.astro</files>
  <read_first>src/pages/Blog.jsx, src/components/blog/BlogPostCard.jsx, 01-PATTERNS.md blog index section, content/categories.json</read_first>
  <action>
    Create src/lib/blog.ts with resolveBlogAsset (from src/blog/utils.js), getCategories/getCategoryLabel (from content/categories.json), estimateReadTime, extractExcerpt fallback — TypeScript or .ts importing existing JS where possible to avoid duplication.

    Create src/lib/posts.ts with sortPostsByDate, groupPostsByYear (from groupPostsByYear.js), filterPosts for static category sections if implemented.

    Create src/components/blog/BlogPostCard.astro mirroring BlogPostCard.jsx: card link href = import.meta.env.BASE_URL + 'blog/' + slug + '/' (trailing slash D-10), cover via resolveBlogAsset, category badges bg-violet-500/10, tags bg-sky-500/10.

    Create minimal src/layouts/BaseLayout.astro stub: html lang zh-CN placeholder, head with charset/viewport only, body slot — full SEO head added in plan 01-03. Import global.css.

    Create src/pages/blog/index.astro: getCollection('posts', ({ data }) => !data.draft), sort by date descending, groupPostsByYear when no filters. Page shell: bg-zinc-900 text-white, max-w-6xl mx-auto px-6 py-16 per Blog.jsx. Static category pill row (links with ?category= or separate paths — Phase 1 may use static sections or query-less all-posts list; at minimum show all published posts in year groups). useDocumentMeta equivalent title 博客, description from SITE.

    Skip DEV editor link, client search, ReadingProgressBar. No posts-index.json fetch.
  </action>
  <acceptance_criteria>
    yarn build produces dist/personalWeb/blog/index.html listing published posts; draft posts absent from list HTML.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && test -f dist/personalWeb/blog/index.html</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Blog detail SSG with Shiki body (D-06, MIGR-02)</name>
  <files>src/pages/blog/[...slug].astro, src/layouts/BlogLayout.astro</files>
  <read_first>src/pages/BlogDetail.jsx, src/components/blog/MarkdownContent.jsx PROSE_CLASS, 01-RESEARCH.md Pattern 3, 01-CONTEXT.md D-06</read_first>
  <action>
    Create minimal src/layouts/BlogLayout.astro stub wrapping slot with bg-zinc-950 min-h-screen — JSON-LD and og:article added in 01-03.

    Create src/pages/blog/[...slug].astro with getStaticPaths from getCollection('posts', ({ data }) => !data.draft), params.slug = post.data.slug. Use render(post) and Content component for markdown body — Shiki highlighting comes from astro.config.mjs markdown.shikiConfig (already set in 01-01).

    Article structure from BlogDetail.jsx: optional cover img with resolveBlogAsset, header h1 title, meta row (author, time dateTime, read time), prose wrapper with classes from MarkdownContent PROSE_CLASS (prose prose-invert prose-lg, code blocks github-dark aligned).

    Skip related posts, BlogPostNav, ReadingProgressBar (deferred CONTEXT). 404 for unknown slug handled by missing path (no static file).

    After build, grep dist/personalWeb/blog/*/index.html for article text from a known post slug (e.g. 2026-06-14-mocode-phase-1) confirming body in HTML not empty root div.
  </action>
  <acceptance_criteria>
    At least one post dist HTML contains h1 title and paragraph text from markdown source; pre/code present when post has fenced code blocks.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && test -f dist/personalWeb/blog/2026-06-14-mocode-phase-1/index.html && grep -q '<h1' dist/personalWeb/blog/2026-06-14-mocode-phase-1/index.html && grep -q '<p' dist/personalWeb/blog/2026-06-14-mocode-phase-1/index.html && grep -q '<pre' dist/personalWeb/blog/2026-06-14-mocode-phase-1/index.html</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/posts/*.md → loader | Untrusted markdown/frontmatter from sync scripts |
| Built HTML → crawlers | Public output must not execute attacker script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW01-03 | Tampering | Markdown body | mitigate | Astro default HTML escape; no rehype-raw; no set:html on post body |
| T-PW01-04 | Tampering | Frontmatter fields | mitigate | Zod schema bounds on collection; optional description only |
| T-PW01-05 | Information disclosure | draft posts | mitigate | Filter !data.draft in getStaticPaths and list getCollection |
</threat_model>

<verification>
yarn test tests/content.schema.test.ts; yarn build; spot-check one blog URL HTML contains full article text.
</verification>

<success_criteria>
MIGR-02: static blog detail HTML with body. MIGR-03: Zod schema + loader. D-06/D-07/D-09 implemented.
</success_criteria>

## Artifacts this phase produces

See plan 01-01 Artifacts table; this plan adds content.config.ts, posts-loader, blog pages, blog components, content.schema tests.

<output>
Create `.planning/phases/PW-01-astro-ssg/01-02-SUMMARY.md` when done
</output>
