---
phase: PW-01-astro-ssg
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/layouts/BaseLayout.astro
  - src/layouts/BlogLayout.astro
  - src/lib/seo.ts
  - src/pages/blog/index.astro
  - src/pages/blog/[...slug].astro
  - tests/seo-head.test.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-06

must_haves:
  truths:
    - "Every built page has html lang=zh-CN"
    - "Every page has title, meta description, and link rel=canonical with trailing slash"
    - "Blog detail pages include og:title, og:description, og:url, og:type article, and BlogPosting JSON-LD"
    - "Canonical URLs use Astro.site + pathname per D-10/D-12"
  artifacts:
    - path: src/layouts/BaseLayout.astro
      provides: "Default SEO head SEO-01 SEO-06"
      contains: "lang=\"zh-CN\""
    - path: src/layouts/BlogLayout.astro
      provides: "Blog OG tags and JSON-LD SEO-02"
      contains: "BlogPosting"
    - path: tests/seo-head.test.ts
      provides: "Automated meta/JSON-LD verification"
  key_links:
    - from: src/layouts/BaseLayout.astro
      to: Astro.site
      via: "new URL(Astro.url.pathname, Astro.site)"
      pattern: "rel=\"canonical\""
    - from: src/layouts/BlogLayout.astro
      to: post.data
      via: "JSON-LD headline datePublished author"
      pattern: "application/ld\\+json"
---

<objective>
Replace layout stubs with full build-time SEO head: canonical URLs with trailing slashes, Open Graph tags, BlogPosting JSON-LD on articles, and zh-CN document language. Implements SEO-01, SEO-02, SEO-06 and D-10/D-11/D-12.

Purpose: Crawlers receive meta in initial HTML — replaces useDocumentMeta client injection.
Output: Enhanced BaseLayout/BlogLayout, seo helpers, seo-head integration tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-01-astro-ssg/01-CONTEXT.md
@.planning/phases/PW-01-astro-ssg/01-RESEARCH.md
@.planning/phases/PW-01-astro-ssg/01-PATTERNS.md
@docs/SEO-MIGRATION-DESIGN.md
@src/hooks/useDocumentMeta.js
@scripts/generate-static.mjs
@src/layouts/BaseLayout.astro
@src/layouts/BlogLayout.astro
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEO helper utilities and seo-head test scaffold</name>
  <files>src/lib/seo.ts, tests/seo-head.test.ts</files>
  <read_first>01-RESEARCH.md Pattern 4, scripts/generate-static.mjs siteUrl, src/hooks/useDocumentMeta.js, 01-CONTEXT.md D-10 D-11 D-12</read_first>
  <behavior>
    - Test after build: dist blog index.html contains lang="zh-CN", meta name=description, link rel=canonical
    - Test after build: one blog post index.html contains og:title, og:type content=article, script type application/ld+json with BlogPosting
    - Test: canonical href ends with trailing slash
  </behavior>
  <action>
    Create src/lib/seo.ts with: formatPageTitle(title, siteName) matching useDocumentMeta pattern (title ? `${title} · ${siteName}` : siteName); buildCanonical(Astro) using new URL(Astro.url.pathname, Astro.site).href ensuring trailing slash consistency with trailingSlash always; buildOgImage(cover) resolving absolute URL via resolveBlogAsset + SITE.url when needed.

    RED: Create tests/seo-head.test.ts that runs yarn build (or reads existing dist if BUILD_SKIP) then parses dist/personalWeb/blog/index.html and one post HTML with regex/cheerio-less string checks for required tags. Document dist path assumptions matching build.format directory.

    Tests fail until Task 2 completes layout wiring — expected RED then GREEN.
  </action>
  <acceptance_criteria>
    tests/seo-head.test.ts exists with assertions for SEO-01, SEO-02, SEO-06; passes after layout task.
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/seo-head.test.ts && test -f src/lib/seo.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: BaseLayout full SEO head (SEO-01, SEO-06, D-10, D-12)</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>index.html, src/index.css, 01-PATTERNS.md BaseLayout section, scripts/lib/site-config.mjs</read_first>
  <action>
    Expand BaseLayout.astro props: title, description, image optional, type default website, noindex optional.

    Set html lang={SITE.language} i.e. zh-CN per SEO-06/D-12. Include charset, viewport, favicon link using import.meta.env.BASE_URL + 'images/favicon.svg', Google fonts links from index.html if still used.

    Head output per D-11/SEO-01: title via formatPageTitle, meta name=description, link rel=canonical href from buildCanonical, og:title, og:description, og:url, og:type, og:locale content zh_CN, og:image when image prop set.

    Body: import global.css, scrollbar classes from index.html html element. Slot for page content. No client-side meta scripts.

    Do not add WebSite/Person JSON-LD here — that is SEO-03 Phase 2.
  </action>
  <acceptance_criteria>
    Built blog list HTML contains lang zh-CN, canonical link with trailing slash, title and description meta tags.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && grep -q 'lang="zh-CN"' dist/personalWeb/blog/index.html && grep -q 'rel="canonical"' dist/personalWeb/blog/index.html</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: BlogLayout OG article tags and BlogPosting JSON-LD (SEO-02, D-11)</name>
  <files>src/layouts/BlogLayout.astro, src/pages/blog/index.astro, src/pages/blog/[...slug].astro</files>
  <read_first>01-RESEARCH.md BlogPosting JSON-LD example, src/pages/BlogDetail.jsx meta fields, 01-CONTEXT.md D-11</read_first>
  <action>
    Expand BlogLayout.astro to accept post collection entry: wrap BaseLayout with type article, title = post.data.title, description = post.data.description or extractExcerpt from body, image = cover resolved.

    Add og:article meta: article:published_time from post.data.date, article:author if applicable.

    Add script type=application/ld+json set:html with JSON.stringify BlogPosting object: @context schema.org, @type BlogPosting, headline, description, datePublished, author Person with name from post.data.author, url canonical, inLanguage zh-CN. Use buildCanonical for url field.

    Wire src/pages/blog/index.astro through BaseLayout only (type website). Wire src/pages/blog/[...slug].astro through BlogLayout with post prop.

    Run yarn test tests/seo-head.test.ts — all assertions green.
  </action>
  <acceptance_criteria>
    Post HTML contains BlogPosting in JSON-LD and og:type article; seo-head.test.ts passes.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && yarn test tests/seo-head.test.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| post.data → JSON-LD set:html | Frontmatter strings embedded in script JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW01-06 | Tampering | JSON-LD set:html | mitigate | JSON.stringify on schema object only; never concatenate raw HTML into script |
| T-PW01-07 | Spoofing | og:url canonical | mitigate | Derive from Astro.site + pathname, not user-supplied absolute URLs in frontmatter |
</threat_model>

<verification>
yarn test tests/seo-head.test.ts passes after full build; manual view-source confirms meta tags without executing JS.
</verification>

<success_criteria>
SEO-01, SEO-02, SEO-06 satisfied on blog list and detail; D-10 trailing slash canonicals consistent.
</success_criteria>

## Artifacts this phase produces

src/lib/seo.ts, full BaseLayout/BlogLayout, tests/seo-head.test.ts — see phase artifacts table in 01-01.

<output>
Create `.planning/phases/PW-01-astro-ssg/01-03-SUMMARY.md` when done
</output>
