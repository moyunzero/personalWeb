---
phase: PW-05-launch-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-production.mjs
  - tests/verify-production.test.ts
  - package.json
  - .github/workflows/deploy.yml
  - public/googleef60eaecd43955c6.html
  - public/BingSiteAuth.xml
autonomous: true
requirements:
  - LAUNCH-03
must_haves:
  truths:
    - "All Phase 1–4 Astro implementation is committed and pushed to origin/master"
    - "GitHub Actions Deploy static content to Pages job succeeds after push"
    - "Production home returns HTTP 200 with Astro zh-CN HTML (not legacy SPA)"
    - "Production googleef60eaecd43955c6.html and BingSiteAuth.xml return 200 with expected bodies"
    - "Production sitemap-index.xml returns HTTP 200"
    - "yarn verify:prod exits 0 against live production URLs"
  artifacts:
    - path: scripts/verify-production.mjs
      provides: "Post-deploy production URL smoke checks"
      exports: ["main"]
    - path: tests/verify-production.test.ts
      provides: "Offline dist verification + optional live gate"
    - path: package.json
      provides: "verify:prod npm script"
      contains: "verify:prod"
  key_links:
    - from: scripts/verify-production.mjs
      to: scripts/lib/site-config.mjs
      via: "SITE.url base for production URLs"
      pattern: "import.*site-config"
    - from: package.json
      to: scripts/verify-production.mjs
      via: "yarn verify:prod"
      pattern: "verify-production"
    - from: .github/workflows/deploy.yml
      to: dist/
      via: "yarn build → upload-pages-artifact"
      pattern: "yarn build"
---

<objective>
Ship uncommitted Astro v1.0 implementation to production via GitHub Pages and add automated post-deploy smoke verification per LAUNCH-03 and D-P5-02/D-P5-03.

Purpose: Production currently serves legacy React SPA; verification files and sitemap-index.xml 404 until implementation is committed and deployed.
Output: Full implementation on origin/master, green GH Actions deploy, scripts/verify-production.mjs + yarn verify:prod passing against live URLs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-05-launch-close/05-RESEARCH.md
@.planning/phases/PW-05-launch-close/05-PATTERNS.md
@.planning/ROADMAP.md
@.github/workflows/deploy.yml
@docs/WEBMASTER-SUBMISSION.md
@scripts/lib/site-config.mjs
@scripts/lib/retry.mjs
@scripts/run-perf-audit.mjs
@scripts/seo-audit.mjs
@tests/phase3-uat-cli.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit all Phase 1–4 implementation with pre-push gate</name>
  <files>astro.config.mjs, src/, scripts/, content/, tests/, public/, package.json, yarn.lock, tailwind.config.js, playwright.config.ts, lighthouserc.json, docs/WEBMASTER-SUBMISSION.md, README.md, .gitignore</files>
  <action>
    Audit `git status`: stage **all** application implementation from Phases 1–4 — Astro src/pages/layouts/components, content/posts, scripts (seo-*, notion-sync, new-post, run-perf-audit), tests, public/ verification files, deploy.yml, package.json, configs. **Do NOT** stage `.astro/`, `.lighthouseci/`, `test-results/`, `dist/`, or planning-only noise unless required for build.

    Per D-P5-01/D-P5-02: confirm deploy.yml triggers on `master` push, runs `yarn build`, uploads `./dist` via deploy-pages. Do not change hosting model or base path.

    Per D-P5-03: `public/googleef60eaecd43955c6.html` and `public/BingSiteAuth.xml` must remain in commit — do not regenerate tokens.

    Pre-push gate (must pass before commit): `yarn build && yarn seo:audit && yarn test:uat:4`. Fix only blocking failures; do not re-implement Phase features.

    Commit message discretion (D-P5 discretion): single `feat: ship Astro v1.0 to GitHub Pages` or logical split — must include all implementation needed for Astro production.

    **Do NOT** push yet — Task 2 handles push after verify-production script exists.
  </action>
  <verify>
    <automated>yarn build && yarn seo:audit && yarn test:uat:4</automated>
  </verify>
  <done>Working tree implementation committed locally; pre-push gate green; verification files tracked in git.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Production smoke script and offline tests</name>
  <files>scripts/verify-production.mjs, tests/verify-production.test.ts, package.json</files>
  <behavior>
    - Test: after yarn build, dist/googleef60eaecd43955c6.html contains google-site-verification
    - Test: after yarn build, dist/BingSiteAuth.xml contains user 4A7B2111
    - Test: package.json scripts.verify:prod points to verify-production.mjs
    - Test: verify-production.mjs defines CHECKS for home-200, google-verify, bing-verify, sitemap-index (offline mock or dist read when --skip-network)
  </behavior>
  <action>
    Create scripts/verify-production.mjs per 05-PATTERNS.md Pattern 1:

    Import SITE from ./lib/site-config.mjs; base = SITE.url (no trailing slash). Import withRetry from ./lib/retry.mjs for network retries (default 3 attempts, 2s backoff) — Pages propagation lag per WEBMASTER-SUBMISSION.md.

    Define **required** CHECKS per user constraint (LAUNCH-03):
    1. `home-200` — GET `${SITE.url}/` → status 200
    2. `google-verify` — GET `${SITE.url}/googleef60eaecd43955c6.html` → status 200, body contains `google-site-verification:`
    3. `bing-verify` — GET `${SITE.url}/BingSiteAuth.xml` → status 200, body contains `<user>`
    4. `sitemap-index` — GET `${SITE.url}/sitemap-index.xml` → status 200

    Use GET not HEAD. Support `--skip-network` flag: validate same assets exist in dist/ after local build (offline CI). Support PRODUCTION_URL env override (same as SITE_URL pattern).

    Findings loop + exit code pattern from seo-audit.mjs: log ERROR/WARN per check; `process.exit(1)` if any error; print summary line `Production verify: N error(s)`.

    **Do NOT** chain into yarn build — standalone post-deploy operator script.

    RED then GREEN: tests/verify-production.test.ts — offline dist checks always run; optional live test behind `PRODUCTION_VERIFY=1` env gate.

    Add package.json: `"verify:prod": "node scripts/verify-production.mjs"`.
  </action>
  <verify>
    <automated>yarn build && yarn test tests/verify-production.test.ts</automated>
  </verify>
  <done>verify-production.mjs implements 4 required live checks; offline tests pass; verify:prod script registered.</done>
</task>

<task type="auto">
  <name>Task 3: Push master, confirm deploy, run live smoke</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
    Include verify-production files in commit if Task 2 created them after Task 1 commit — amend only if HEAD commit is unpushed and created by this session; otherwise new commit.

    Push `master` to `origin` per D-P5-02. Monitor GitHub Actions workflow **Deploy static content to Pages** until success (use `gh run watch` or Actions UI). Wait 1–3 minutes for Pages CDN propagation.

    Run live smoke: `yarn verify:prod`. On transient failure, retry with script's built-in retries; if still failing, inspect Actions logs — do not proceed to Phase 5 plan 02.

    Confirm legacy SPA symptoms gone: production home should NOT have `lang="en"` SPA redirect script; `sitemap-index.xml` must return 200 (not 404).

    Update docs/WEBMASTER-SUBMISSION.md **部署顺序** section only if needed: note Astro ship commit, `yarn verify:prod` as post-deploy gate, keep existing verification file URLs unchanged.
  </action>
  <verify>
    <automated>yarn verify:prod 2>&1 | grep -q 'Production verify: 0 error'</automated>
  </verify>
  <done>LAUNCH-03 satisfied: Astro dist live on GitHub Pages; verification files and sitemap-index.xml return 200; yarn verify:prod exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git → origin/master | Push triggers production deploy — only application code |
| verify-production → production URLs | Untrusted network; read-only GET probes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW05-01 | Tampering | deploy.yml branch trigger | mitigate | Push only to master; verify workflow unchanged from D-P5-02 |
| T-PW05-02 | Information Disclosure | Verification files in public/ | accept | Intentionally public ownership tokens per D-P5-03 |
| T-PW05-03 | Denial of Service | Removing verification files post-deploy | mitigate | Keep G/Bing files permanently in public/ per RESEARCH Pitfall 3 |
| T-PW05-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 adds no new packages (RESEARCH Package Legitimacy Audit skipped) |
</threat_model>

<verification>
yarn build && yarn test:uat:4; git push master; GH Actions deploy green; yarn verify:prod 0 errors on live production.
</verification>

<success_criteria>
LAUNCH-03: Production serves Astro dist with google/Bing verification files and sitemap-index.xml live at documented URLs; legacy SPA replaced.
</success_criteria>

<output>
Create `.planning/phases/PW-05-launch-close/05-01-SUMMARY.md` when done
</output>
