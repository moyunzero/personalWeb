---
phase: PW-04-content-launch
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/pages/index.astro
  - src/layouts/BaseLayout.astro
  - src/components/islands/ParticleIsland.tsx
  - lighthouserc.json
  - docs/WEBMASTER-SUBMISSION.md
  - README.md
  - package.json
  - scripts/run-perf-audit.mjs
autonomous: false
requirements:
  - LAUNCH-01
  - LAUNCH-02
user_setup:
  - service: google-search-console
    why: "LAUNCH-01 sitemap submission"
    dashboard_config:
      - task: "Add property https://moyunzero.github.io/personalWeb/ and submit sitemap-index.xml"
        location: "Google Search Console → Sitemaps"
  - service: bing-webmaster
    why: "LAUNCH-01 sitemap submission"
    dashboard_config:
      - task: "Submit same sitemap URL"
        location: "Bing Webmaster Tools → Sitemaps"
  - service: baidu-webmaster
    why: "LAUNCH-01 sitemap submission"
    dashboard_config:
      - task: "Submit sitemap + optional push"
        location: "百度站长平台"
must_haves:
  truths:
    - "Home Lighthouse Performance ≥ 85 and LCP < 2500 ms on mobile simulated preset (D-P4-05)"
    - "yarn perf:audit runs LHCI against preview — NOT chained into yarn build (D-P4-11)"
    - "Phaser remains click-to-load; GameIsland dynamic import unchanged (D-P4-10)"
    - "docs/WEBMASTER-SUBMISSION.md documents Google + Bing + Baidu sitemap steps (D-P4-06)"
    - "Rich Results Test URLs for 3 sample posts documented in webmaster doc (D-P4-07)"
    - "README links to webmaster submission guide"
  artifacts:
    - path: lighthouserc.json
      provides: "LHCI assert Performance ≥0.85, LCP ≤2500"
    - path: docs/WEBMASTER-SUBMISSION.md
      provides: "三平台站长提交 + launch checklist"
    - path: package.json
      provides: "perf:audit and test:uat:4 scripts"
  key_links:
    - from: package.json
      to: lighthouserc.json
      via: "perf:audit → lhci autorun"
    - from: docs/WEBMASTER-SUBMISSION.md
      to: src/lib/site.ts
      via: "SITE.origin + basePath sitemap URL"
    - from: src/pages/index.astro
      to: src/components/islands/GameIsland.tsx
      via: "client:only preserved; other islands client:visible"
---

<objective>
Hit home performance targets, document webmaster submission for three platforms, and complete launch verification per LAUNCH-01 and LAUNCH-02.

Purpose: Close the migration with measurable perf gate and operator-ready launch docs; manual webmaster steps require user accounts.
Output: Island/font perf fixes, lighthouserc.json, yarn perf:audit, WEBMASTER-SUBMISSION.md, human launch checkpoint. E2e regression lives in 04-01 Task 4.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-04-content-launch/04-RESEARCH.md
@.planning/phases/PW-04-content-launch/04-PATTERNS.md
@.planning/phases/PW-04-content-launch/04-VALIDATION.md
@scripts/lib/site-config.mjs
@src/pages/index.astro
@src/layouts/BaseLayout.astro
@src/components/islands/GameIsland.tsx
@.planning/phases/PW-04-content-launch/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home performance — island deferral + font/icon optimization</name>
  <files>src/pages/index.astro, src/layouts/BaseLayout.astro, src/components/islands/ParticleIsland.tsx</files>
  <read_first>04-RESEARCH.md Performance scope, GameIsland.tsx click gate, index.astro current directives, 02-RESEARCH.md client:visible pattern</read_first>
  <action>
    Per D-P4-05 and D-P4-10, apply surgical perf fixes (baseline Performance 77, LCP 3753 ms):

    index.astro: HomeMotion client:only → client:visible; ParticleIsland client:only → client:visible; MouseTrailIsland client:only → client:idle; ContactIsland client:load → client:visible. Keep GameIsland client:only with existing click-to-load Phaser dynamic import — verify no top-level phaser import.

    BaseLayout.astro: reduce render-blocking fonts — preload Montserrat 600 + Lora 400 only; defer Material Symbols or replace 4 home icon usages with inline SVG (remove full icon font link if feasible). Use font-display: swap.

    ParticleIsland.tsx: reduce max particle count (150 → 60 per RESEARCH) for LCP headroom.

    Do NOT remove Phaser, service workers, or CDN. Do NOT add homepage featured posts (D-P4-08). Re-run Phase 2 game smoke after changes.
  </action>
  <acceptance_criteria>
    Home static HTML renders hero without waiting for GSAP/canvas; GameIsland button visible before hydration; no Phaser in initial bundle.
  </acceptance_criteria>
  <verify>
    <automated>yarn build:astro 2>&1 | tail -3 | grep -q 'Complete'</automated>
  </verify>
  <done>Island and font optimizations applied without GameIsland regression.</done>
</task>

<task type="auto">
  <name>Task 2: LHCI config + yarn perf:audit (separate from build)</name>
  <files>lighthouserc.json, package.json</files>
  <read_first>04-RESEARCH.md LHCI assert config, Package Legitimacy Audit table, D-P4-11</read_first>
  <action>
    Add devDependencies: lighthouse@13.4.0, @lhci/cli@0.15.1 (Approved per RESEARCH Package Legitimacy Audit).

    Create lighthouserc.json: collect url http://localhost:4321/personalWeb/, numberOfRuns 3, settings preset perf (mobile simulated throttling per LAUNCH-02). assert: categories:performance minScore 0.85; largest-contentful-paint maxNumericValue 2500.

    Add scripts: "perf:audit": "lhci autorun" (document prerequisite: yarn preview running on :4321). Add "test:uat:4": vitest top-n + related tests + playwright phase4 spec.

    Do NOT append perf:audit to yarn build — build stays seo:audit && astro build only (D-P4-11).

    Create scripts/run-perf-audit.mjs: spawn `yarn preview` if port 4321 not responding, wait until http://localhost:4321/personalWeb/ returns 200, run `lhci autorun`, exit with LHCI code. Wire package.json "perf:audit": "node scripts/run-perf-audit.mjs".

    Run yarn build then yarn perf:audit — iterate Task 1 fixes until thresholds pass. Task is NOT done until LAUNCH-02 thresholds met (D-P4-05): Performance ≥ 85, LCP &lt; 2500 ms. No SUMMARY escape hatch for failing perf.
  </action>
  <acceptance_criteria>
    lighthouserc.json exists; yarn perf:audit exits 0 with Performance ≥ 0.85 and LCP ≤ 2500 ms (mobile simulated preset).
  </acceptance_criteria>
  <verify>
    <automated>test -f lighthouserc.json && node -e "const p=require('./package.json'); if(!p.scripts['perf:audit']) process.exit(1); if(p.scripts.build.includes('perf:audit')) process.exit(1)" && yarn build && yarn perf:audit</automated>
  </verify>
  <done>LAUNCH-02 perf gate passes thresholds via repeatable yarn perf:audit (outside build chain).</done>
</task>

<task type="auto">
  <name>Task 3: Webmaster docs + test:uat:4 + README link</name>
  <files>docs/WEBMASTER-SUBMISSION.md, README.md, package.json</files>
  <read_first>scripts/lib/site-config.mjs origin/basePath, 04-RESEARCH.md Webmaster docs structure, src/pages/robots.txt.ts</read_first>
  <action>
    Create docs/WEBMASTER-SUBMISSION.md per D-P4-06 and D-P4-07:

    Sections: 站点信息 (origin https://moyunzero.github.io, base /personalWeb, sitemap-index.xml, robots.txt URLs); Google Search Console steps; 必应 Webmaster Tools; 百度站长平台 (note github.io indexing limits).

    Rich Results 抽查 table with 3 concrete blog URLs (e.g. 2023-03-12-html, 2023-04-20-typescript, 2025-05-20-rag) + expected BlogPosting type.

    Launch checklist: yarn build 0 error, yarn seo:audit 0 error, yarn perf:audit pass,三平台 sitemap submitted, Rich Results 3 posts.

    Update README.md §部署/SEO: link to docs/WEBMASTER-SUBMISSION.md; list yarn perf:audit and yarn seo:top-n-score scripts. Do NOT reference BlogEditor or /blog/editor.

    Add package.json "test:uat:4": vitest run tests/related-posts.test.ts tests/top-n-score.test.ts tests/top-n-checklist.test.ts && playwright test tests/e2e/phase4-launch-uat.spec.ts. Run yarn test:uat:4 after Task 1 perf changes to confirm D-P4-10 no regression.
  </action>
  <acceptance_criteria>
    docs/WEBMASTER-SUBMISSION.md exists with three platform sections and 3 Rich Results URLs; README links to it; yarn test:uat:4 passes.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/WEBMASTER-SUBMISSION.md && grep -q 'WEBMASTER-SUBMISSION' README.md && yarn build && yarn test:uat:4</automated>
  </verify>
  <done>LAUNCH-01 documentation complete; Phase 4 automated UAT green post-perf.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual webmaster submission + Rich Results verification</name>
  <files>docs/WEBMASTER-SUBMISSION.md</files>
  <what-built>Performance optimizations, perf:audit script, WEBMASTER-SUBMISSION.md with platform steps and 3 Rich Results URLs, full build+audit+e2e green.</what-built>
  <how-to-verify>
    1. Run `yarn build && yarn seo:audit` — confirm 0 errors.
    2. Start `yarn preview`, run `yarn perf:audit` — confirm Performance ≥ 85 and LCP &lt; 2.5s.
    3. Follow docs/WEBMASTER-SUBMISSION.md: submit sitemap-index.xml to Google Search Console, Bing Webmaster, 百度站长.
    4. Open Google Rich Results Test for each of the 3 documented blog URLs — confirm BlogPosting valid.
    5. Check launch checklist items in WEBMASTER-SUBMISSION.md.
  </how-to-verify>
  <resume-signal>Type "approved" when platforms submitted and Rich Results pass, or describe blockers.</resume-signal>
  <acceptance_criteria>
    User confirms三平台 sitemap submitted and 3 Rich Results tests pass per D-P4-06/D-P4-07.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && yarn seo:audit</automated>
  </verify>
  <done>LAUNCH-01 and LAUNCH-02 human verification complete; Phase 4 ready to ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External webmaster platforms | User credentials; docs only in repo |
| npm devDeps → local audit | lighthouse/@lhci/cli from approved registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW04-05 | Spoofing | npm packages lighthouse/@lhci/cli | mitigate | RESEARCH Package Legitimacy Audit Approved; pin versions in package.json |
| T-PW04-06 | Denial of Service | CI perf gate | accept | perf:audit NOT in build/deploy CI — local/manual only (D-P4-11) |
| T-PW04-SC | Tampering | npm installs | mitigate | Only lighthouse + @lhci/cli added; slopcheck via legitimacy audit |
</threat_model>

<verification>
yarn build; yarn seo:audit; yarn preview + yarn perf:audit green; yarn test:uat:4; docs/WEBMASTER-SUBMISSION.md complete; user checkpoint for webmaster + Rich Results.
</verification>

<success_criteria>
LAUNCH-02: perf:audit passes thresholds. LAUNCH-01: webmaster doc + user submission confirmed. GameIsland click-to-load preserved. No perf script in yarn build.
</success_criteria>

<output>
Create `.planning/phases/PW-04-content-launch/04-03-SUMMARY.md` when done
</output>
