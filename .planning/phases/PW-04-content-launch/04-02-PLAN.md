---
phase: PW-04-content-launch
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scripts/lib/top-n-score.mjs
  - scripts/seo-top-n-score.mjs
  - scripts/seo-top-n-checklist.mjs
  - content/top-n-queue.json
  - content/posts/*.md
  - tests/top-n-score.test.ts
  - tests/top-n-checklist.test.ts
  - package.json
autonomous: true
requirements:
  - OPT-03
  - OPT-04
must_haves:
  truths:
    - "Top 15–20 posts ranked by no-GA formula: keyword×2 + quality×2 + hub×1 (D-P4-03)"
    - "Queue artifact content/top-n-queue.json lists ranked slugs with scores and method no-ga-fallback-v1"
    - "yarn seo:top-n-score --write generates queue; default --dry-run prints table without writing"
    - "Top N posts pass seo-top-n-checklist.mjs with 0 errors after content edits (D-P4-04)"
    - "Each Top N post has title ≤60, description 120–160, ≥2 H2 headings, ≥2 outbound /blog/ inlinks, cover alt"
    - "Top N set has hub-and-spoke cross-links for inbound inlink requirement"
  artifacts:
    - path: scripts/lib/top-n-score.mjs
      provides: "Pure scorePost + rankPosts functions"
      exports: ["scorePost", "rankPosts", "TECH_KEYWORDS"]
    - path: scripts/seo-top-n-score.mjs
      provides: "CLI --limit --dry-run --write"
    - path: scripts/seo-top-n-checklist.mjs
      provides: "Checklist validation scoped to queue slugs"
    - path: content/top-n-queue.json
      provides: "Ranked Top 15–20 optimization queue"
    - path: tests/top-n-score.test.ts
      provides: "Scoring unit tests"
  key_links:
    - from: scripts/seo-top-n-score.mjs
      to: scripts/lib/scan-posts.mjs
      via: "scanPosts() for corpus"
    - from: scripts/seo-top-n-score.mjs
      to: scripts/lib/top-n-score.mjs
      via: "rankPosts(published, { limit })"
    - from: scripts/seo-top-n-checklist.mjs
      to: content/top-n-queue.json
      via: "read queue slugs only"
    - from: scripts/lib/top-n-score.mjs
      to: scripts/lib/meta-rules.mjs
      via: "isHashSlug for quality_score penalty"
---

<objective>
Implement Top N scoring CLI and execute deep optimization checklist on 15–20 ranked posts per OPT-03 and OPT-04.

Purpose: Prioritize corpus optimization without GA; validate editorial quality via automated checklist on queue slugs only.
Output: top-n-score.mjs, seo-top-n-score.mjs, seo-top-n-checklist.mjs, top-n-queue.json, content edits, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-04-content-launch/04-RESEARCH.md
@.planning/phases/PW-04-content-launch/04-PATTERNS.md
@.planning/phases/PW-04-content-launch/04-VALIDATION.md
@docs/SEO-MIGRATION-DESIGN.md
@scripts/lib/scan-posts.mjs
@scripts/lib/meta-rules.mjs
@scripts/seo-audit.mjs
@.planning/phases/PW-04-content-launch/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — top-n scoring pure functions + unit tests</name>
  <files>scripts/lib/top-n-score.mjs, tests/top-n-score.test.ts</files>
  <read_first>04-RESEARCH.md Top N scoring table, scripts/lib/meta-rules.mjs isHashSlug, docs/SEO-MIGRATION-DESIGN.md §5</read_first>
  <behavior>
    - Test: keyword_score 0–3 when title matches TECH_KEYWORDS (javascript, typescript, react, ai, rag, node, html, css, astro, langchain, etc.)
    - Test: quality_score +2 body stripped length > 6000; +1 description 120–160; +1 non-hash slug via isHashSlug
    - Test: hub_score +1 category note; +1 when ≥3 tags
    - Test: total score = keyword×2 + quality×2 + hub×1 (no traffic_rank — D-P4-03)
    - Test: rankPosts returns top N sorted by score desc, tie-break publishedAt desc
  </behavior>
  <action>
    RED: Create tests/top-n-score.test.ts importing from scripts/lib/top-n-score.mjs.

    GREEN: Implement scripts/lib/top-n-score.mjs exporting TECH_KEYWORDS array, scorePost(post) returning { score, breakdown: { keyword, quality, hub } }, rankPosts(posts, { limit: 20 }). Post shape from scanPosts: { data: { slug, title, description, categories, tags, publishedAt, draft }, content }.

    Import isHashSlug from meta-rules.mjs — do not duplicate. Max keyword_score 3, quality 4, hub 2 per RESEARCH. No GA/traffic_rank (ANAL-01 deferred v2).

    Do NOT batch-rename slugs (D-P4-09).
  </action>
  <acceptance_criteria>
    Scoring formula matches RESEARCH no-GA fallback; unit tests pass.
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/top-n-score.test.ts</automated>
  </verify>
  <done>Pure scoring module tested and ready for CLI wiring.</done>
</task>

<task type="auto">
  <name>Task 2: seo-top-n-score CLI + queue JSON + npm script</name>
  <files>scripts/seo-top-n-score.mjs, content/top-n-queue.json, package.json</files>
  <read_first>scripts/seo-audit.mjs shebang/exit pattern, seo-meta-batch.mjs parseArgs, 04-RESEARCH.md queue JSON shape</read_first>
  <action>
    Create scripts/seo-top-n-score.mjs: scanPosts → filter !draft → rankPosts(limit default 20, clamp 15–20 via --limit=). Flags: --dry-run (default, print TSV score/slug/title, exit 0); --write (save content/top-n-queue.json with generatedAt, method: no-ga-fallback-v1, posts: [{ slug, score, rank, title }]).

    Add package.json script: "seo:top-n-score": "node scripts/seo-top-n-score.mjs". Do NOT add to yarn build chain (D-P4-11 pattern).

    Run --write once to commit initial queue artifact. Verify limit yields 15–20 rows per D-P4-03.
  </action>
  <acceptance_criteria>
    yarn seo:top-n-score --dry-run exits 0 with ranked table; --write creates valid JSON with 15–20 entries.
  </acceptance_criteria>
  <verify>
    <automated>yarn seo:top-n-score --dry-run | grep -q 'Top' && yarn seo:top-n-score --write && node -e "const q=require('./content/top-n-queue.json'); if(q.posts.length<15||q.posts.length>20) process.exit(1)"</automated>
  </verify>
  <done>OPT-03 scoring CLI and queue artifact committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: seo-top-n-checklist.mjs + unit tests</name>
  <files>scripts/seo-top-n-checklist.mjs, tests/top-n-checklist.test.ts</files>
  <read_first>04-RESEARCH.md Depth optimization checklist table, scripts/seo-audit.mjs output tokens</read_first>
  <behavior>
    - Test: passes fixture post with title ≤60, description 120–160, ≥2 ## headings, ≥2 markdown links matching /blog/{slug}/
    - Test: fails/warns when description out of range or fewer than 2 H2
    - Test: only validates slugs present in queue file — ignores non-queue posts
  </behavior>
  <action>
    Create scripts/seo-top-n-checklist.mjs reading content/top-n-queue.json. For each queued slug: load post via scanPosts find by slug; apply rules per D-P4-04:

    title ≤60 + contains keyword from TECH_KEYWORDS → warn if fail; description 120–160 → warn; body ≥2 ## headings → warn; ≥2 outbound markdown links to /blog/ paths → warn; coverImage set → warn/error if detail would use empty alt (check frontmatter cover + title fallback).

    Inbound inlinks (≥3 other Top-N posts link to slug): report as manual-review info line, not hard error — cross-link task covers this.

    Exit 1 on error severity; exit 0 with warn count printed. Stable grep tokens: Checklist errors: N, Checklist warnings: N.

    RED then GREEN: tests/top-n-checklist.test.ts with inline fixture posts.
  </action>
  <acceptance_criteria>
    Checklist script runs against queue; unit tests pass; non-queue posts not scanned.
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/top-n-checklist.test.ts && node scripts/seo-top-n-checklist.mjs 2>&1 | grep -q 'Checklist'</automated>
  </verify>
  <done>OPT-04 automated checklist ready for content edit validation.</done>
</task>

<task type="auto">
  <name>Task 4: Deep-optimize Top N content + hub-and-spoke cross-links</name>
  <files>content/posts/*.md, package.json</files>
  <read_first>content/top-n-queue.json slugs, 04-RESEARCH.md OPT-04 checklist + Pitfall 5 inbound inlinks, Phase 3 parseFrontmatter pattern</read_first>
  <action>
    For each slug in top-n-queue.json (15–20 posts per D-P4-04): edit markdown:

    - Title ≤60 chars with primary TECH_KEYWORD where natural
    - Description 120–160 chars (prefer intentional copy over extractExcerpt)
    - Body: ≥2 ## section headings; add 1–2 contextual markdown links to /blog/{slug}/ posts
    - parseFrontmatter → mutate → stringifyFrontmatter only (no slug renames — D-P4-09)

    Hub-and-spoke: ensure each queued slug receives ≥3 inbound markdown links from other queue posts. Document inbound counts in checklist manual-review lines.

    Add package.json script if missing: "seo:top-n-checklist": "node scripts/seo-top-n-checklist.mjs".

  Re-run seo-top-n-checklist.mjs — target 0 errors. yarn seo:audit must stay 0 errors. yarn build green. Do NOT add homepage featured posts (D-P4-08).
  </action>
  <acceptance_criteria>
    All queue slugs pass checklist with 0 errors; inbound manual lines show ≥3 for each slug; build and audit green.
  </acceptance_criteria>
  <verify>
    <automated>yarn build && yarn seo:audit && node scripts/seo-top-n-checklist.mjs; test $? -eq 0</automated>
  </verify>
  <done>OPT-03/OPT-04 complete — ranked queue optimized, cross-linked, and validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → content/posts/*.md | Writes only via established frontmatter round-trip |
| Queue JSON → checklist scope | Only listed slugs validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW04-03 | Tampering | Content post writes | mitigate | parseFrontmatter/stringifyFrontmatter only; no slug changes (D-P4-09) |
| T-PW04-04 | Tampering | Path traversal in CLI | mitigate | Reuse scan-posts resolved paths under content/posts |
| T-PW04-SC | Tampering | npm installs | accept | Plan 04-02 adds no new packages |
</threat_model>

<verification>
yarn test tests/top-n-score.test.ts tests/top-n-checklist.test.ts; yarn seo:top-n-score --write; content edits on 15–20 posts; node scripts/seo-top-n-checklist.mjs exits 0; yarn build green.
</verification>

<success_criteria>
OPT-03: 15–20 posts ranked and queued. OPT-04: queue posts pass checklist with cross-linked inbound graph; seo:audit unchanged at 0 errors.
</success_criteria>

<output>
Create `.planning/phases/PW-04-content-launch/04-02-SUMMARY.md` when done
</output>
