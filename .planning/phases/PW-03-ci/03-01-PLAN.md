---
phase: PW-03-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/post-schema.mjs
  - scripts/lib/scan-posts.mjs
  - scripts/seo-meta-batch.mjs
  - tests/post-schema.test.ts
  - tests/meta-batch.test.ts
  - package.json
autonomous: true
requirements:
  - FLOW-03
must_haves:
  truths:
    - "yarn seo:meta-batch --dry-run scans all 96 posts and reports description gaps without writing files"
    - "yarn seo:meta-batch --apply fills empty published descriptions from extractExcerpt(body, 160)"
    - "Default batch run never changes slug or renames files (D-P3-02)"
    - "Only --force-slug may rename slug and matching .md file"
    - "Batch writes use parseFrontmatter → mutate data → stringifyFrontmatter only"
  artifacts:
    - path: scripts/lib/post-schema.mjs
      provides: "Canonical Zod postDataSchema (non-strict until plan 03-03)"
      exports: ["postDataSchema", "validatePostData"]
    - path: scripts/lib/scan-posts.mjs
      provides: "Enumerate content/posts/*.md with raw frontmatter"
      exports: ["scanPosts"]
    - path: scripts/seo-meta-batch.mjs
      provides: "CLI --dry-run / --apply / --force-slug"
    - path: tests/meta-batch.test.ts
      provides: "Slug guard + dry-run/apply behavior"
  key_links:
    - from: scripts/seo-meta-batch.mjs
      to: scripts/lib/scan-posts.mjs
      via: "scanPosts()"
    - from: scripts/seo-meta-batch.mjs
      to: src/blog/frontmatter.js
      via: "parseFrontmatter + stringifyFrontmatter on apply"
    - from: scripts/seo-meta-batch.mjs
      to: src/blog/excerpt.js
      via: "extractExcerpt(content, 160) when description empty"
---

<objective>
Implement `yarn seo:meta-batch` with scan, gap report, safe apply, and slug protection per D-P3-02 and D-P3-03.

Purpose: Enable scalable metadata maintenance for 96-post corpus before audit gate and strict schema (plans 03-02/03-03).
Output: Shared post schema (non-strict), scan-posts helper, seo-meta-batch CLI, unit tests, package.json script.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-03-ci/03-RESEARCH.md
@.planning/phases/PW-03-ci/03-PATTERNS.md
@.planning/phases/PW-03-ci/03-VALIDATION.md
@.planning/ROADMAP.md
@scripts/lib/load-posts.mjs
@scripts/notion-sync.mjs
@src/blog/frontmatter.js
@src/blog/excerpt.js
@src/loaders/posts-loader.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared post schema and scan-posts foundation</name>
  <files>scripts/lib/post-schema.mjs, scripts/lib/scan-posts.mjs, tests/post-schema.test.ts</files>
  <read_first>src/loaders/posts-loader.ts lines 48-65, 03-RESEARCH.md Pattern 1, 03-PATTERNS.md post-schema section</read_first>
  <behavior>
    - Test: validatePostData accepts published post with non-empty description
    - Test: validatePostData accepts draft with empty description (draft exemption per D-P3-01 discretion)
    - Test: scanPosts returns array with file, filePath, fileSlug, data, content for each .md under content/posts/
  </behavior>
  <action>
    Create scripts/lib/post-schema.mjs exporting postDataSchema and validatePostData(). Mirror loader field list (slug, title, description, publishedAt, categories, tags, draft, featured, author, etc.). Use z from zod directly. Non-strict mode: description is z.string() with superRefine that only enforces non-empty description when POST_SCHEMA_STRICT=1 AND draft is false (strict flip deferred to plan 03-03 per D-P3-01).

    Create scripts/lib/scan-posts.mjs: resolve POSTS_DIR to content/posts relative to repo root; readdir filter .md; parseFrontmatter per file; return { file, filePath, fileSlug, data, content, raw }. Do NOT call extractExcerpt here — audit and batch need raw YAML description.

    RED then GREEN: tests/post-schema.test.ts for schema parse + draft exemption; add scanPosts smoke test with fixture or mock if needed.
  </action>
  <acceptance_criteria>
    postDataSchema validates loader-equivalent fields; draft posts allow empty description in non-strict mode; scanPosts enumerates only content/posts/*.md with parsed frontmatter.
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/post-schema.test.ts</automated>
  </verify>
  <done>Shared schema and scan helper exist with passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seo-meta-batch CLI with slug protection</name>
  <files>scripts/seo-meta-batch.mjs, tests/meta-batch.test.ts</files>
  <read_first>scripts/notion-sync.mjs parseCli + write pattern, 03-RESEARCH.md Pattern 2, D-P3-02, D-P3-03</read_first>
  <behavior>
    - Test: default/--dry-run does not write files
    - Test: --apply fills empty description from excerpt, preserves notionId and other keys
    - Test: slug unchanged without --force-slug even when hash-like
  </behavior>
  <action>
    Create scripts/seo-meta-batch.mjs with shebang. Parse flags: default and --dry-run identical (report only, exit 0); --apply writes; --force-slug opt-in for slug rename + file rename only when explicitly passed (D-P3-02).

    RED then GREEN: tests/meta-batch.test.ts using Vitest temp fixture directory — create .md with empty description and body; assert dry-run leaves file unchanged; assert --apply sets description from excerpt; assert slug unchanged without --force-slug; assert notionId preserved when present.

    For each post from scanPosts(): report empty/whitespace description; warn title.length > 60 without auto-truncate; suggest readable slug for hash-like slugs but never apply slug change unless --force-slug.

    On --apply: resolve write path with path.resolve and verify resolved path starts with resolved POSTS_DIR — reject .. traversal (T-PW03-01). Mutate only data.description when empty using extractExcerpt(content, 160). Use parseFrontmatter → mutate data object → stringifyFrontmatter(content, data) — never regex-splice --- blocks (T-PW03-03, D-P3-03). Preserve all existing frontmatter keys (notionId, notionSyncedAt, cover, etc.).

    Print summary lines with stable tokens for CI grep: `Posts scanned: N`, `Would update (description): N` (dry-run) or `Updated (description): N` (apply), plus Warnings count. Exit 1 only on IO/parse failures; dry-run always exit 0.
  </action>
  <acceptance_criteria>
    Dry-run reports gaps for empty descriptions; apply writes only under content/posts/; slug and filename unchanged unless --force-slug; YAML round-trip preserves unrelated keys.
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/meta-batch.test.ts</automated>
  </verify>
  <done>seo-meta-batch CLI implements scan, suggest, apply, and slug protection per locked decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Register seo:meta-batch npm script</name>
  <files>package.json</files>
  <read_first>package.json scripts block, 03-VALIDATION.md Wave 0, D-P3-05</read_first>
  <action>
    Add to package.json: "seo:meta-batch": "node scripts/seo-meta-batch.mjs". Do NOT change `build` script yet — audit/build chain deferred until corpus batch apply in plan 03-03 Task 1 (avoids CI red on ~32 empty descriptions mid-phase).

    Smoke: yarn seo:meta-batch --dry-run exits 0 and prints `Posts scanned:` with count ≥ 90.
  </action>
  <acceptance_criteria>
    package.json contains seo:meta-batch script; yarn seo:meta-batch --dry-run runs without writing files; build script unchanged from pre-plan state.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.scripts['seo:meta-batch']!=='node scripts/seo-meta-batch.mjs') process.exit(1)" && yarn seo:meta-batch --dry-run | grep -q 'Posts scanned'</automated>
  </verify>
  <done>FLOW-03 script registered; dry-run usable on full corpus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → filesystem | Untrusted paths must not escape content/posts |
| Frontmatter strings → YAML | User/title content must not break YAML structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW03-01 | Tampering | seo-meta-batch writes | mitigate | path.resolve + prefix check POSTS_DIR; reject .. segments |
| T-PW03-02 | Tampering | slug rename | mitigate | Default no slug change; --force-slug required (D-P3-02) |
| T-PW03-03 | Tampering | YAML corruption on apply | mitigate | parseFrontmatter/stringifyFrontmatter only; JSON.stringify for generated strings in tests |
| T-PW03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 adds no new packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
yarn test tests/post-schema.test.ts tests/meta-batch.test.ts; yarn seo:meta-batch --dry-run shows description gaps; no files modified after dry-run (git diff content/posts empty).
</verification>

<success_criteria>
FLOW-03: seo:meta-batch supports --dry-run and --apply with slug frozen by default; shared schema and scan helpers ready for audit plan 03-02.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| Post schema (non-strict) | scripts/lib/post-schema.mjs | FLOW-03 foundation |
| Post scanner | scripts/lib/scan-posts.mjs | FLOW-03 |
| Meta-batch CLI | scripts/seo-meta-batch.mjs | FLOW-03, D-P3-03 |
| Schema tests | tests/post-schema.test.ts | Wave 0 |
| Batch tests | tests/meta-batch.test.ts | FLOW-03, T-PW03-01/02 |
| npm script | package.json `seo:meta-batch` | D-P3-03 |

<output>
Create `.planning/phases/PW-03-ci/03-01-SUMMARY.md` when done
</output>
