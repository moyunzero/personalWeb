---
phase: PW-03-ci
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - scripts/lib/meta-rules.mjs
  - scripts/seo-audit.mjs
  - scripts/new-post.mjs
  - scripts/notion-sync.mjs
  - tests/seo-audit.test.ts
  - tests/post-schema.test.ts
  - package.json
autonomous: true
requirements:
  - SEO-05
  - FLOW-01
  - FLOW-02
must_haves:
  truths:
    - "yarn seo:audit exits 1 when published post has empty frontmatter description"
    - "yarn seo:audit exits 0 when only warnings present"
    - "yarn seo:audit registered as standalone script (build chain deferred to 03-03 after batch apply)"
    - "yarn blog:new creates draft with non-empty description stub (FLOW-01)"
    - "yarn notion:sync fills empty Notion description from body excerpt (FLOW-02)"
    - "GitHub Actions deploy.yml unchanged — yarn build chain satisfies SEO-05"
  artifacts:
    - path: scripts/lib/meta-rules.mjs
      provides: "runAuditRules with error/warn severity table"
      exports: ["runAuditRules"]
    - path: scripts/seo-audit.mjs
      provides: "Pre-build SEO gate CLI"
    - path: tests/seo-audit.test.ts
      provides: "Severity and exit-code coverage"
  key_links:
    - from: package.json
      to: scripts/seo-audit.mjs
      via: "seo:audit script (build chain in 03-03)"
    - from: scripts/seo-audit.mjs
      to: scripts/lib/meta-rules.mjs
      via: "runAuditRules(scanPosts())"
    - from: scripts/new-post.mjs
      to: scripts/lib/post-schema.mjs
      via: "validatePostData on generated frontmatter (test)"
---

<objective>
Implement `yarn seo:audit`, wire it into `yarn build` for CI gate (SEO-05, D-P3-04, D-P3-05), and update content workflows so new posts and Notion sync output schema-compatible frontmatter (FLOW-01, FLOW-02).

Purpose: Block SEO regressions in local build and GitHub Actions before corpus batch apply and strict schema.
Output: meta-rules, seo-audit CLI, updated blog:new + notion:sync, build script chain, tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PW-03-ci/03-RESEARCH.md
@.planning/phases/PW-03-ci/03-PATTERNS.md
@.planning/phases/PW-03-ci/03-VALIDATION.md
@.planning/phases/PW-03-ci/03-01-SUMMARY.md
@scripts/new-post.mjs
@scripts/notion-sync.mjs
@scripts/lib/post-schema.mjs
@scripts/lib/scan-posts.mjs
@.github/workflows/deploy.yml
@src/blog/getCategories.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: meta-rules and seo-audit CLI</name>
  <files>scripts/lib/meta-rules.mjs, scripts/seo-audit.mjs, tests/seo-audit.test.ts</files>
  <read_first>03-RESEARCH.md Pattern 3 audit table, src/blog/getCategories.js validateCategoryIds, scripts/lib/site-config.mjs</read_first>
  <behavior>
    - Test: published post empty description → error desc-missing
    - Test: draft empty description → no desc-missing error
    - Test: title missing → error; title length > 60 → warn only
    - Test: unknown category id → error categories-invalid
    - Test: SITE.description empty → error site-config
  </behavior>
  <action>
    Create scripts/lib/meta-rules.mjs exporting runAuditRules(posts) returning { file, rule, severity: 'error'|'warn', message }[]. Implement rules from RESEARCH Pattern 3: desc-missing (error, published only), desc-short/desc-long (warn), title-missing (error), title-long (warn), slug-filename/slug-hash (warn), date-missing (error), categories-invalid (error via getCategories Set), site-config (error if SITE.description empty). Draft posts exempt from desc-missing (D-P3-01 discretion).

    Create scripts/seo-audit.mjs: scanPosts → runAuditRules → print findings → exit(errors.length > 0 ? 1 : 0). Warnings do not fail build (D-P3-04).

    RED then GREEN: tests/seo-audit.test.ts unit-tests runAuditRules fixtures; optional minimal subprocess test for exit code.
  </action>
  <acceptance_criteria>
    runAuditRules returns correct severity per rule table; seo:audit exits 1 on any error-level finding, 0 on warnings-only or clean.
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/seo-audit.test.ts</automated>
  </verify>
  <done>SEO-05 audit engine implemented with automated severity tests.</done>
</task>

<task type="auto">
  <name>Task 2: Register seo:audit npm script</name>
  <files>package.json</files>
  <read_first>package.json scripts, .github/workflows/deploy.yml Build step, D-P3-04</read_first>
  <action>
    Add package.json script: "seo:audit": "node scripts/seo-audit.mjs". Keep existing seo:meta-batch from plan 03-01. Do NOT change `build` yet — chaining audit before astro build is deferred to plan 03-03 Task 1 after corpus `--apply` so CI/deploy stays green mid-phase (~32 empty descriptions would fail audit today).

    Do NOT edit deploy.yml. Verify yarn seo:audit standalone runs and exits non-zero while corpus still has empty published descriptions (expected until plan 03-03 batch).
  </action>
  <acceptance_criteria>
    package.json contains seo:audit script; build script unchanged (still astro build only); deploy.yml unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.scripts['seo:audit']!=='node scripts/seo-audit.mjs') process.exit(1); if(p.scripts.build!=='astro build') process.exit(1)" && grep -q 'yarn build' .github/workflows/deploy.yml</automated>
  </verify>
  <done>SEO-05 audit CLI registered; build chain wired after batch in 03-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update blog:new for schema-compatible frontmatter</name>
  <files>scripts/new-post.mjs, tests/post-schema.test.ts</files>
  <read_first>scripts/new-post.mjs lines 66-77, 03-PATTERNS.md new-post section, D-P3-03, D-P3-06</read_first>
  <behavior>
    - Test: generated template frontmatter passes validatePostData with draft:true and non-empty description
    - Test: description stub comes from extractExcerpt on template body, not empty string
  </behavior>
  <action>
    Update scripts/new-post.mjs (FLOW-01, D-P3-03): import extractExcerpt from ../src/blog/excerpt.js. Replace description: "" with description: JSON.stringify(extractExcerpt(`# ${title}\n\n在这里开始写作…`, 120)). Keep draft: true, JSON.stringify(title) for YAML safety, existing slug generation unchanged.

    Add tests/post-schema.test.ts describe block blog:new validating parsed template object against validatePostData.

    Do NOT add homepage featured posts or web editor paths (D-P3-08, D-P3-06).
  </action>
  <acceptance_criteria>
    blog:new template produces non-empty description stub; validatePostData succeeds for draft posts; no description: "" in new templates.
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/post-schema.test.ts -t blog:new</automated>
  </verify>
  <done>FLOW-01: new posts ship with valid frontmatter from creation.</done>
</task>

<task type="auto">
  <name>Task 4: Update notion:sync description fallback</name>
  <files>scripts/notion-sync.mjs, tests/post-schema.test.ts</files>
  <read_first>scripts/notion-sync.mjs lines 145-331, 03-PATTERNS.md notion-sync section, FLOW-02</read_first>
  <action>
    Update scripts/notion-sync.mjs (FLOW-02, D-P3-03): import extractExcerpt. After body fetch, if !meta.description.trim(), set description = extractExcerpt(body, 160) before frontmatter object. Keep stringifyFrontmatter write path unchanged; preserve notionId, notionSyncedAt, slug registry behavior.

    Import validatePostData from ./lib/post-schema.mjs; log Zod errors as warnings before write if validation fails — still write with filled description so sync is not blocked.

    Add tests/post-schema.test.ts describe notion: fixture frontmatter shape from sync path validates with validatePostData.

    Do NOT bulk rename slugs in sync (existing behavior); meta-batch slug policy separate (D-P3-02).
  </action>
  <acceptance_criteria>
    Notion posts without Description property get excerpt-filled description in written frontmatter; validatePostData passes on typical sync output shape.
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/post-schema.test.ts -t notion</automated>
  </verify>
  <done>FLOW-02: notion:sync output compatible with Content Collections schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontmatter fields → audit rules | Raw YAML is source of truth, not parsePost excerpt |
| Notion API → local md write | External content normalized before write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PW03-01 | Tampering | notion-sync / new-post writes | mitigate | Writes only to content/posts/; existing path checks preserved |
| T-PW03-02 | Tampering | slug rename | mitigate | notion-sync keeps ensureUniqueSlug; no batch slug changes (D-P3-02) |
| T-PW03-03 | Tampering | YAML corruption | mitigate | stringifyFrontmatter for all writes; JSON.stringify on generated strings |
| T-PW03-04 | Repudiation | audit bypass | mitigate | yarn build chains seo:audit; build:astro documented escape only |
</threat_model>

<verification>
yarn test tests/seo-audit.test.ts tests/post-schema.test.ts; yarn seo:audit runs (may exit 1 pre-batch — expected); yarn build:astro succeeds; package.json build script chains audit.
</verification>

<success_criteria>
SEO-05 audit gate active in build; FLOW-01 blog:new and FLOW-02 notion:sync produce schema-valid frontmatter; deploy.yml needs no change.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| Audit rules | scripts/lib/meta-rules.mjs | SEO-05 |
| Audit CLI | scripts/seo-audit.mjs | SEO-05, D-P3-04 |
| Audit tests | tests/seo-audit.test.ts | SEO-05 |
| Updated blog:new | scripts/new-post.mjs | FLOW-01, D-P3-03 |
| Updated notion:sync | scripts/notion-sync.mjs | FLOW-02, D-P3-03 |
| Audit npm script | package.json seo:audit | SEO-05, D-P3-04 |

<output>
Create `.planning/phases/PW-03-ci/03-02-SUMMARY.md` when done
</output>
