---
title: 弹性溢出滚动
slug: 2024-09-02-36ddf5c0
description: 滚动到视区的顶部或底部边缘之外几百个像素，松开该按钮会将页面对齐回原位。
author: 墨韵
date: 2024-09-02
categories:
  - note
tags:
  - frontend
  - CSS
draft: false
notionId: 36ddf5c0-26f4-80d5-88ac-d4d522300024
notionSyncedAt: 2026-05-27T04:40:27.303Z
---

使用**纯 CSS 的解决方案**


下面是一个包含一些子元素的容器：


```javascript
<div class="carousel">
  <div class="slides">
    <div class="slide">1</div>
    <div class="slide">2</div>
    <div class="slide">3</div>
    <div class="slide">4</div>
    <div class="slide">5</div>
  </div>
</div>
```


设置一些基本样式，特别是创建一种保证父容器溢出的情况。


```javascript
.carousel {
  width: 200px;
  height: 400px;
  overflow-x: hidden;
  overflow-y: auto;
}

.slides {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap;
  width: 100%;
  height: fit-content;
}

.slide {
  width: 100%;
  aspect-ratio: 1;
}
```

> aspect-ratio 是一个 CSS 属性，用于设置或获取元素的纵横比（ width / height 比率）。当指定了 aspect-ratio 时，浏览器会根据该比例计算元素的宽度和高度，以保持指定的宽高比。

然后添加一些垂直边距。如果容器只有一个项，将其添加到子元素的顶部和底部。如果容器有多个子元素，则需要在第一个子元素的顶部和最后一个子元素的底部添加 margin。


```javascript
.carousel > .slides > .slide:first-child {
  margin-top: 100px;
}

.carousel > .slides > .slide:last-child {
  margin-bottom: 100px;
}
```


现在可以滚动越过边缘，然后需要将其捕捉回来。需要 使用[scroll-snap-type](https://css-tricks.com/almanac/properties/s/scroll-snap-type/) 和 [scroll-snap-align](https://css-tricks.com/almanac/properties/s/scroll-snap-align/) 属性


```javascript
.carousel {
  scroll-snap-type: y mandatory;
}

.carousel > .slides > .slide {
  scroll-snap-align: start;
}

.carousel > .slides > .slide:first-child {
  margin-top: 100px;
}

.carousel > .slides > .slide:last-child {
  scroll-snap-align: end;
  margin-bottom: 100px;
}
```

> scroll-snap-type 设置了在有滚动容器的情形下吸附至吸附点的严格程度。
> - mandatory
>
> 若滚动容器当前未在滚动，则其可见视口必须吸附至吸附位置。
>
> - proximity
>
> 若滚动容器当前未在滚动，则其可见视口可以吸附至吸附位置。是否吸附由用户代理根据滚动参数决定。若指定了吸附轴，则此为默认的吸附程度。
>
>
> -
>
>
> **scroll-snap-align** 属性将盒子的吸附位置指定为其吸附区域（作为对齐对象）在其吸附容器的吸附口（作为对齐容器）中的对齐方式。其两值分别指定了在块向轴盒行向轴上的吸附对齐方式。若仅指定一值，则第二值默认为同一值。
>
> - none
>
> 此盒在此轴上未定义吸附位置。
>
> - start
>
> 此盒的滚动吸附区域在滚动容器的吸附口中的首对齐为此轴上的吸附位置。
>
> - end
>
> 此盒的滚动吸附区域在滚动容器的吸附口中的末对齐为此轴上的吸附位置。
>
> - center
>
> 此盒的滚动吸附区域在滚动容器的吸附口中的居中对齐为此轴上的吸附位置。
>
>

---


参考文章：


[https://css-tricks.com/elastic-overflow-scrolling/](https://css-tricks.com/elastic-overflow-scrolling/)